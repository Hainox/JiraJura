# Доска задач

Статусы: `planned` → `in_progress` → `review` → `approved` (PR смержен) →
`deployed` (подтверждено на проде). Кто начинает задачу — добавляет/обновляет
строку. Не нужно заводить строку на каждый мелкий фикс — только на то, что
может пересечься с работой другого агента, или что стоит отслеживать
дольше одной сессии.

| Задача | Статус | Кто | PR | Заметки |
|---|---|---|---|---|
| Визуальная группировка таблицы «По районам» | `in_progress` | Codex CLI | — | Ветка `codex/group-statistics-columns`: группы «Площадки», «Обходы», «Замечания»; build/lint/tests пройдены, ожидает push/PR |
| Координация агентов (AGENTS.md, этот каталог) | `in_progress` | Claude Code | — | Согласовано с Codex CLI 24.08 (см. диалог с владельцем продукта) |
| Автоповтор загрузки фото замечания | `approved` | Claude Code | #108 | Смержено; команда деплоя на `obhod-sao.ru` отправлена владельцу продукта, подтверждения деплоя пока нет |
| «Ошибка при сохранении фото устранения» (обращение 21.08, Остропольцев) | `planned` | — | — | Второе из двух связанных обращений про фото — не разбиралось отдельно, в отличие от «фото ДО» |
| Statistics v2 (модель данных, `/api/v1/stats`, PPTX, 5 вкладок) | `deployed` | Codex CLI + владелец продукта | #107 и др. | `docs/STATS_MODEL_V2.md` — полностью реализовано и задеплоено, подтверждено `verification-report-2026-08-21.md` (87/87 backend, 31/31 frontend) |
| Зачистка старого сервера (journal.yuvibot2.xyz, 82.23.173.215) | `planned` | Claude Code (скрипт) / владелец продукта (запуск) | — | Скрипт `full-wipe-jirajura-old-server.sh` подготовлен и отправлен, требует SSH-доступа, которого у агентов нет — ждёт запуска вручную |

## Handoff

Заметки для следующей сессии (своей или другого агента) — что важно знать,
прежде чем продолжать.

**2026-08-24, Claude Code:**
- Только что заведён этот протокол координации — `AGENTS.md` ещё не
  проверялся в реальной параллельной работе, возможны доработки по ходу.
- Второе обращение про фото («ошибка при сохранении фото устранения») не
  разбиралось — начинать оттуда, если продолжаете тему фото; вероятно
  смежный класс проблемы с тем, что уже исправлено в #108 (флоу «фото
  ПОСЛЕ»/исправление у reviewer/admin — другой код-путь, не проверялся).
- Локальный git в облачной песочнице Claude Code периодически откатывается
  к старому коммиту между сообщениями — если что-то не сходится с этой
  доской, довериться `git log` на GitHub (или свежему `git clone`), не
  локальному рабочему каталогу. Подробности — `CLAUDE.md`.
